<h1>Update due date</h1>

<div id="item_profile">
	<% lender = User.find(@item.user_id)%>
	<p>Lender: <%= lender.fname %> <%= lender.lname %></p>
	<p>Item: <%= @item.name %></p>
	<%= render partial: 'items/item_profile' %>
</div>

<%= render 'due_date_form' %>

<%= link_to '< Back', :back %>